[profile.default]
src = "src"
out = "out"
libs = ["lib"]
ffi = true

# See more config options https://github.com/foundry-rs/foundry/blob/master/crates/config/README.md#all-options
# A product of this is a Foundry Framework needs to have FFI enabled to allow it to write new files.
